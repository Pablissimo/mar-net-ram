#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../kyuri/bin/kyuri" "$@"
  ret=$?
else 
  node  "$basedir/../kyuri/bin/kyuri" "$@"
  ret=$?
fi
exit $ret
